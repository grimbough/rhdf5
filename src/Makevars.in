RHDF5_LIBS=`echo 'Rhdf5lib::pkgconfig("PKG_C_LIBS")'|\
    "${R_HOME}/bin/R" --vanilla --slave`
HDF5FILTER_LIBS=`echo 'hdf5Filters::pkgconfig("PKG_LIB")'|\
    "${R_HOME}/bin/R" --vanilla --slave`
PKG_LIBS=$(RHDF5_LIBS) @INCLUDE_PATH@
